//Change "yourIP" to the IP address of the computer that is hosting the application.
const yourIP = "192.168.0.100";

const config = {
    serverUrl: 'http://'+ yourIP +':5000',
  };
  
  export default config;